# Changelog

本文件记录 SpiritVale 简体中文社区补丁的公开版本。版本号遵循 `MAJOR.MINOR.PATCH`，发布日期使用 UTC 日期。

## [1.2.38] - 2026-08-20

### Fixed

- 修复 Windows PowerShell 5.1 向 Python `-c` 传参时剥离引号，导致 runner 的 Python 版本验证语法错误的问题。
- Python 3.12 验证改为解析解释器原生 `--version` 输出，避免 shell 引号差异。

### Verified

- 新版本检查已在 Windows PowerShell 5.1 和 runner 私有 Python `3.12.10` 上实测通过。
- 保持 `v1.2.34` 已实机验证的运行时插件和玩家功能不变；本版仅修订安装器及自动发布链路。

## [1.2.37] - 2026-08-19

### Fixed

- 修复 `setup-python` 在非管理员 self-hosted Windows runner 上尝试全局安装并挂起至超时的问题。
- Release Workflow 现在验证并使用 runner 私有的 Python 3.12，审计依赖、源数据审计和打包使用同一解释器。

### Verified

- runner 私有 Python `3.12.10` 与锁定的 UnityPy `1.25.3` 已完成安装和导入验证。
- 保持 `v1.2.34` 已实机验证的运行时插件和玩家功能不变；本版继续修订安装器及自动发布链路。

## [1.2.36] - 2026-08-19

### Fixed

- 修复 Release Workflow 在 job 级引用 `runner.temp` 导致 GitHub 在进入任何 job 前拒绝执行的问题。
- 将 .NET 安装目录配置移动到实际可用 `runner` context 的 `setup-dotnet` 步骤。

### Verified

- Release Workflow 已通过 `actionlint 1.7.12` 和仓库 Workflow 策略测试。
- 保持 `v1.2.34` 已实机验证的运行时插件和玩家功能不变；本版仅修订安装器及自动发布链路。

## [1.2.35] - 2026-08-19

### Fixed

- 修复 Windows Release Workflow 在干净检出环境中缺少 Python 审计依赖、生成审计文件和 NuGet 还原的问题。
- 修复安装器自检在 GitHub runner 深层工作目录下触发 Windows 路径长度限制的问题。
- Release 现在由专用 Windows runner 生成冻结候选，再由 GitHub Actions 校验并发布资产。

### Verified

- 保持 `v1.2.34` 已实机验证的运行时插件、中文拍卖行搜索、`Tab` 中英文切换和帧率优化内容不变。
- 在干净检出环境中完成源数据审计、Release 构建、标准安装器自检和兼容包自检。

## [1.2.34] - 2026-08-18

### Added

- 将词条品质评分和星级标记集成到共享运行时，装备与神器详情可复用缓存结果，避免逐帧重复计算。
- 词条品质标记会随紧凑界面的中英文切换保留，不会重复叠加星标。

### Fixed

- 修复新版拍卖行只接受英文 `SearchRequest.Query` 导致中文搜索为空的问题。
- 中文完整名称和歧义短词现在会使用有界英文候选查询，保留高级筛选并合并去重结果；英文搜索仍由游戏原生处理。
- 适配 Steam Build `24773178` 新增的 5 个运行时实体显示别名。

### Verified

- RuntimeLocalization 测试 `5793` 项、BilingualDisplay 测试 `75` 项、实体目录测试 `6` 项、市场搜索测试 `11742` 项、AgentLoopSafety `12` 项全部通过。
- 当前构建包含 `5229` 条运行时翻译和 `3624` 条双语实体目录记录。
- Release 插件构建 `0` 错误；实机冷启动日志确认新版市场请求桥接已命中 `SearchRequest.Query`。

## [1.2.33] - 2026-08-08

### Fixed

- 修复安装器生成的默认配置未启用 `Tab` 中英文切换，导致安装版无法直接切换的问题。
- 新安装和修复安装现在默认使用 `CompactSurfaceMode = EnglishToggle`，每按一次 `Tab` 即可在中文和英文实体名称之间切换。

### Verified

- 安装器自检通过，安装清单和载荷配置均记录 `EnglishToggle` 与 `Tab`。
- 完成安装后的冷启动实机验证，技能名称可通过 `Tab` 在中英文之间往返切换。
- 保留 `v1.2.31` 已验证的性能优化和 Steam Build `24534566` 运行时载荷。

## [1.2.31] - 2026-08-07

### Changed

- 优化运行时字体回退与动态中文字形缓存，减少重复字形填充和 UI 文本处理造成的帧率抖动。
- 复用共享的动态中文字体，避免多个 TMP 字体重复生成相同字形。
- 修复最新版市场数据接口变化导致的兼容性问题。
- 保留中英文显示模式和 `Tab` 切换能力。

### Performance

- 实机运行帧率提升 20 多帧。
- 字体处理开销从每 5 秒 `125–382 ms` 降至稳定的 `3–5 ms`，降低约 96%–99%。
- 初次字体预热耗时约 `26.883 ms`。

### Verified

- 适配 Steam Build `24534566`，完成两次冷启动并检查服务器列表与运行日志。
- 双语显示测试：75 项通过。
- 市场功能测试：11,738 项通过。
- Release 构建：0 错误。
- 实机加载 5,224 条翻译，中文文本无缺字方框，运行日志无性能或字体回退错误。

## [1.2.30] - 2026-07-29

### Changed

- 安装器默认使用纯中文显示模式，实体名称和紧凑界面优先显示中文。
- 需要英文对照时，可将 `CompactSurfaceMode` 设为 `EnglishToggle`，再按 `Tab` 切换紧凑界面的中文/英文名称。
- 同时提供标准安装器 EXE 与 Windows x64 兼容版压缩包。

### Verified

- 适配当前 Steam Build `24302330`。
- 完成两次冷启动，并检查服务器列表、必需游戏界面和运行日志。
- 验证 5,224 条运行时翻译和 3,615 条双语实体目录记录。

## [1.2.29] - 2026-07-23

### Verified

- 完成当前 Steam Build `24302330` 的双冷启动、运行日志和必需界面实机验证。
- 验证 5,224 条运行时翻译和 3,615 条双语实体目录记录。
